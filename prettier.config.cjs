/** @typedef  {import("prettier").Config} PrettierConfig*/

/** @type { PrettierConfig } */
const config = {
  arrowParens: 'always',
  trailingComma: 'all',
  singleQuote: true,
  tabWidth: 2,
  printWidth: 100,
};

module.exports = config;
